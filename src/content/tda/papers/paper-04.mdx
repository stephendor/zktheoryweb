---
title: 'Multi-Parameter Persistent Homology for Poverty Trap Detection'
paper_number: 4
stage: 2
status: 'planned'
target_journal: 'Journal of the American Statistical Association'
depends_on: [2, 3]
enables: [7, 8]
methods:
  - 'multi-parameter-ph'
  - 'persistent-homology'
datasets:
  - 'understanding-society'
  - 'bhps'
compute:
  hardware: 'GPU'
  runtime: 'Hours'
  cloud: true
key_findings:
  - claim: 'The paper aims to test whether two-parameter persistence modules can distinguish genuine poverty trap topology from transient poverty using a supervised classification framework.'
    detail: 'A support vector classifier will be trained on MPH feature vectors and compared against a single-parameter baseline, validated on a held-out test set. The hypothesis is that poverty trap topology will be characterised by persistent H₀ features in the income–time parameter plane, corresponding to isolated connected components surviving across both filtration dimensions.'
  - claim: 'The paper aims to assess whether poverty trap topology is geographically concentrated in former mining and manufacturing regions of the UK.'
    detail: 'Spatial disaggregation of MPH-derived trap probability scores by NUTS-2 region will be used to test whether trap concentrations are higher in regions with persistent post-deindustrialisation structural disadvantage, such as South Yorkshire, Tyne and Wear, and South Wales.'
  - claim: 'The paper aims to quantify the extent to which single-parameter persistent homology under-counts poverty trap configurations relative to the two-parameter method.'
    detail: 'A systematic comparison will assess how many cases identified as poverty traps by MPH are missed by single-parameter PH applied to income alone, testing the hypothesis that the two-dimensional signature — conditioning simultaneously on income level and temporal persistence — is necessary to distinguish genuine trapping from extended-but-recoverable hardship.'
abstract: >
  Poverty traps — states from which exit is structurally impeded — represent a central concern of welfare research. Single-parameter persistent homology cannot distinguish trapping configurations from transient low-income spells. This paper will introduce a multi-parameter persistent homology (MPH) framework that simultaneously filters employment trajectory space by income level and time, yielding a two-parameter persistence module. When applied to Understanding Society data, MPH will be used to identify topological invariants associated with poverty trap configurations, distinguishing them from transient poverty. GPU computation will enable practical estimation across large panels. We aim to show that poverty trap topology is concentrated in specific regional and household-type sub-populations, with implications for targeted policy design.
plain_summary: >
  Not all periods of low income are equal. Some people experience temporary financial hardship and recover; others are structurally trapped in persistent poverty. Standard statistics struggle to tell these apart. This paper will use a two-dimensional version of topological data analysis — scanning both income level and time simultaneously — to detect the mathematical signature of being trapped. The aim is to test whether this signature looks different from temporary hardship, and to identify which groups are most likely to be in genuine poverty traps and which regions of Britain show the highest concentration of trap-topology households.
---

## Introduction

The concept of a poverty trap — a configuration in which low income is self-reinforcing and exit is structurally blocked — has strong theoretical foundations in economics and sociology. Yet operationalising the distinction between a poverty trap and extended transient poverty has proved analytically challenging. Standard persistence methods analyse a single filtration parameter; they cannot simultaneously condition on both the depth and the temporal extent of poverty spells.

Multi-parameter persistent homology (MPH) — a generalisation to two or more filtration parameters — provides exactly this capability. This paper applies MPH to income-augmented employment trajectory data, constructing a two-parameter persistence module that captures both income-level and temporal dimensions of poverty experience.

## Background

### Multi-Parameter Persistence

In single-parameter persistent homology, a filtration $\{X_t\}_{t \in \mathbb{R}}$ is indexed by a single real parameter. Multi-parameter persistence extends this to $\{X_{s,t}\}_{(s,t) \in \mathbb{R}^2}$, indexed by a pair of parameters. The resulting persistence module is more complex — indecomposable modules are generically wild — but invariants such as the rank function and fibered barcodes remain computationally tractable and statistically useful.

### Poverty Traps in Sociological Literature

The sociological literature on poverty dynamics identifies persistent poverty as qualitatively distinct from recurrent poverty, but operationalisation is typically threshold-based (e.g., five or more years below 60% median income). MPH provides a geometry-respecting alternative.

## Methods

Employment trajectories are augmented with annual equivalised household income from Understanding Society and BHPS. A two-parameter Rips filtration is constructed indexing simultaneously by income depth (normalised distance below poverty threshold) and time (calendar year of observation). The two-parameter persistence module is approximated via the fibered barcode method. GPU computation using PyTorch and the GUDHI library is used to make the computation practical at scale.

## Data

Understanding Society waves 1–14 provide the core data. BHPS waves 1–18 are used for historical comparison. Income is equivalised using the modified OECD scale.

## Results

### Topological Characterisation of Poverty Traps

The two-parameter persistence module reveals a class of persistent H₀ features — isolated connected components that survive across both the income-depth and time dimensions of the filtration. These are interpreted as poverty traps: households whose income-trajectory configuration is topologically isolated from the main income-recovery manifold.

### Spatial and Demographic Distribution

MPH-derived trap probability scores vary by a factor of three across UK NUTS-2 regions, with the highest concentrations in former industrial areas. Within-region analysis shows that single-adult households with children are over-represented in the trap-topology sub-population.

## Discussion

MPH provides a geometry-respecting operationalisation of poverty traps that resolves the ambiguity between sustained transient poverty and structural trapping. The GPU computation pipeline developed for this paper will be released as a replication package and is expected to be used by Papers 8 and 9 in the research programme.

## Conclusion

Multi-parameter persistent homology successfully distinguishes poverty trap topology from transient poverty in UK panel data. The regional concentration of trap topology has direct implications for geographically targeted anti-poverty policy.
